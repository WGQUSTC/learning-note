EHSM和caliptra都是对flash验签和远程认证（当然也有VM对设备的安全认证，目前只是对flash的固件认真保证是可信的没有被修改过），类似SSH https://www.ruanyifeng.com/blog/2011/08/what_is_a_digital_signature.html，本质都是加解密算法
低功耗问题：
  1. ICG和enhance CG有什么区别，（ICG-3bit， enhance CG 6bit）？
      会有更多插门控的可能，对应DC ultra模式，比如不同寄存器使用相同门控使能时，可以把这些寄存器拼凑够min bit width的要求
  2. 嵌套的else if是否会被门控？
      会
  3. X态率要求<3%，X态会影响什么？
      x态只有0/1-x和x-0/1的，中间的x态都没有动态功耗
  4. ICG插入的原则，多少bit会插入icg依照面积评估还是功耗来评估，和工艺是否有关系
      min bit的限制考虑是面积，max bit是timing。max bit是因为单个使能信号驱动的bit过多，影响timing（考虑因为是fanout过大影响的timing），后端要求手工拆小，要么允许综合器自动复制使能条件，需要和后端对齐，然后就可以把这个检查关掉。一般icg cell也包含max fanout，pa算法会根据这个fanout来判断是否插入
