IOMMU proposal：
https://lists.riscv.org/g/sig-datacenter/message/183
https://github.com/riscv-non-isa/riscv-iommu/blob/main/iommu_ref_model/libiommu/src/iommu_translate.c
IOMMU软件使用：
1. vcpu超分：分配给虚拟机的 vCPU 数量，超过了宿主机实际可用的物理 CPU（pCPU）数量
2. mrif使用场景：当cpu超分场景时，有一些vcpu可能分配不到irq file，通过iommu把信息写到内存中，然后通过iommu发送中断给host，host收到中断之后调度vcpu去响应mrif，这样即使vcpu没有分配到irq file，也可以读到想要的信息
3. 虚拟机直通：设备直通是指设备直接分配给虚拟机，此时stage2是要使用的。stage1是相当于host虚拟给vm的翻译，前提是vm支持viommu，其实用的就是stage1。
4. 虚拟化：VM内部也想用iommu：其实就是host虚拟出来给vm用的，软件看来就是viommu，实际是iommu的stage1.  vf发出gva经过viommu翻译成gpa，每次配置viommu都会trap到host，其实就是配置stage1.
5. 初始化：小核初始化生成iommu拓扑信息，配置Iommu.bare-->大核在coreboot根据拓扑信息生成acpi-->opensbi--->linux boot--->kernal/os解析acpi，初始化iommu
6. 在开启t2gpa=1，iommu返回的是gpa而不是spa。这样的话p2p是不允许直接使用gpa的需要通过pcie acs强制走iommu。Hypervisors that configure T2GPA to 1 must ensure through protocol-specific means that translated accesses are routed through the host such that the IOMMU may translate the GPA and then route the transaction based on PA to memory or to a peer device. For PCIe, for example, the Access Control Service (ACS) must be configured to always redirect peer-to-peer (P2P) requests upstream to the host.
7. 一般只有在虚拟机重启的时候会发起inv，但是有多个虚拟机的时候一个虚拟机重启，其他的虚拟机还在使用，那么就可能出现正在进行的翻译时发起的invalidation
8. PCIE直通就是H2H，4x4lane

IOMMUkernal和驱动：
1. 驱动和kernal的理解参照https://www.cnblogs.com/polin/p/17159858.html
2. 初始化：识别寄存器地址范围，中断模式，注册虚拟中断号，申请内存，创建页表，创建cq/fq/pq，注册pmu中断处理函数，注册子系统
3. firmware给kernal的信息：1个是RIMT表描述RC到IOMMU的ID mapping tree，另一个是DSDT表描述设备的资源和本体。驱动执行的操作：
   a. 从acpi也就是firmware获取寄存器地址范围
   b. 注册中断
   c. 申请内存，创建DDT页表，配置ddtp
   d. 申请内存创建cq,fq,pq等
4. ARM的FEAT_HDBSS是记录stage2产生的dirty page，方便虚拟机热迁移时拷贝。热迁移是为了保证VM从一个HOST切换到另一个host时不感知中断，分为pre copy and post copy。使用precopy时HDBSS就有用了



IOMMU community issue summary：
1. get attribute重写priv=pidv & priv ？ s ：u。在翻译完成的时候，rsp的priv=orig_pidv & priv。假如pidv=0,priv=1那么在iommu中当成非特权，返回的时候用请求方的方式返回
2. https://github.com/riscv-non-isa/riscv-iommu/issues/750    意思是如果带s，会把range内的都清掉。不带s的，只会对addr相关的路径无效


需要注意的点：
1. 总共dtim的trans token不超过256，每个inv token是16
2. laid是unique的，来自与pcie哪里需要确认的
3. cache indx对于bdf和segment的hash是有区别的，bus num权重更大
4. napot和svadu只更新第一个entry
5. 带宽计算公式---outstanding x burstlen x datawidth = expectationBW x roundtrip latency
6. ptw是64bit也就是8byte对齐的，假如要读取的地址分别是{vpn1,vpn0}和{vpn1,vpn0+8}那么最终操作得到的ptw addr也是地址相差8，ptw entry相邻，所以可以一起读回来
7. 带宽计算：gen6 pciex16lane最大是128GB，如果读写同时存在的话，这128GB大部分都是写data占据的，而读请求包占128GB的比例很小。但是到iommu感知到的带宽就是128GBX2=256GB
